# reduxcache/config.py
# Default configuration settings

# Directory where cache files (if using disk persistence) will be stored.
DEFAULT_CACHE_DIR = "./redux_cache"

# Expiry time for cache entries in seconds (e.g. 1 hour).
DEFAULT_CACHE_EXPIRY = 3600
